# NextAuth.js 配置
# 复制此文件为 .env 并填入实际值

# ==================== Google OAuth配置 ====================
# 在Google Cloud Console中设置重定向URI为: http://localhost:3000/api/auth/callback/google
# 重要：确保Google OAuth客户端配置正确，支持PKCE
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ==================== NextAuth配置 ====================
# NEXTAUTH_URL 是您的应用基础URL
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# ==================== OAuth路径配置 ====================
# 登录入口: POST /api/auth/signin/google (后端兼容接口)
# 回调地址: /api/auth/callback/google (NextAuth自动处理)
# 安全机制: PKCE (Proof Key for Code Exchange) 已启用
# Cookies配置: 已优化状态管理和PKCE验证
# 接口方式: 使用fetch POST请求启动OAuth流程

# ==================== 其他配置 ====================
NODE_ENV=development
