# Sino-Name 项目环境变量配置
# 复制此文件为 .env 并填入实际值

# ==================== Google OAuth配置 ====================
# 在Google Cloud Console中设置重定向URI为: http://localhost:3001/api/auth/callback/google
# 重要：确保Google OAuth客户端配置正确
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ==================== NextAuth配置 ====================
# NEXTAUTH_URL 是后端服务的基础URL（端口3001）
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your_nextauth_secret_here

# ==================== 前端配置 ====================
# FRONTEND_BASE_URL 是前端服务的基础URL（端口3000）
FRONTEND_BASE_URL=http://localhost:3000

# ==================== OAuth路径配置 ====================
# 登录入口: POST /api/auth/signin/google
# 回调地址: /api/auth/callback/google (后端处理)
# 用户信息: GET /api/auth/user
# 登出接口: POST /api/auth/logout
# 错误信息: GET /api/auth/oauth-error

# ==================== 安全配置 ====================
# CSRF保护: 使用state参数防止跨站请求伪造
# 会话管理: HttpOnly Cookie + Base64编码
# 状态验证: 自动验证OAuth state参数

# ==================== 其他配置 ====================
NODE_ENV=development

# ==================== 开发环境说明 ====================
# 前端服务运行在端口 3000
# 后端服务运行在端口 3001
# Google OAuth回调到后端端口 3001
# 前端页面显示在端口 3000
